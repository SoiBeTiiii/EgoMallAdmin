'use client';

import { MyActionIconDelete } from "aq-fe-framework/components";

export default function F_6j8jkftgnc_Delete({ id }: { id: string }) {
    return (
        <>
            <MyActionIconDelete onSubmit={() => { }} contextData={id}></MyActionIconDelete>
        </>
    );
}