import MyActionIconDelete from "@/components/ActionIcons/ActionIconCRUD/MyActionIconDelete";

export default function F_nxiyjlnrik_Delete({ id }: { id: string }) {
  return <MyActionIconDelete contextData={id} onSubmit={() => {}} />;
}
